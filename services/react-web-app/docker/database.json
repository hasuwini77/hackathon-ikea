{
  "bucket": "ikea_products",
  "guest": {
    "disabled": false,
    "admin_channels": ["*"]
  },
  "num_index_replicas": 0,
  "enable_shared_bucket_access": true,
  "import_docs": true,
  "sync": "function(doc, oldDoc) { channel('!'); access('*', '*'); }"
}
